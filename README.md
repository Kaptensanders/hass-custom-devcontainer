# A docker container for bootstrapping Home Assistant development of plugins and integrations

Originally this was forked from @thomasloven, but has drifted a lot since then

Use the helper script to build, then use image in local devcontainers
